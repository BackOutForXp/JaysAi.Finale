<!-- neural v3.0 -->
<UserControl x:Class="JaysAi.Finale.UI.Panels.CrosshairSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid Background="Transparent">
        <StackPanel Margin="10">
            <TextBlock Text="Crosshair Settings" FontWeight="Bold" FontSize="16" Margin="0 0 0 10" />

            <CheckBox Content="Enable Crosshair" IsChecked="{Binding EnableCrosshair}" Margin="0 0 0 5"/>
            <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                <TextBlock Text="Color:" VerticalAlignment="Center" Width="60"/>
                <TextBox Width="100" Text="{Binding CrosshairColor}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                <TextBlock Text="Size:" VerticalAlignment="Center" Width="60"/>
                <Slider Width="150" Minimum="1" Maximum="20" Value="{Binding CrosshairSize}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Opacity:" VerticalAlignment="Center" Width="60"/>
                <Slider Width="150" Minimum="0" Maximum="1" Value="{Binding CrosshairOpacity}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
