<!-- neural v3.0 -->
<UserControl x:Class="JaysAi.Finale.UI.Settings.ProfileManager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="600"
             Background="#1E1E1E">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Profile Manager" FontSize="18" FontWeight="Bold"
                   Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <ListBox x:Name="ProfileList" Grid.Row="1" Background="#2A2A2A" Foreground="White"
                 BorderThickness="0" Margin="0,0,0,10" Height="250"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Load" Width="80" Margin="5" Click="Load_Click"/>
            <Button Content="Save" Width="80" Margin="5" Click="Save_Click"/>
            <Button Content="Delete" Width="80" Margin="5" Click="Delete_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
